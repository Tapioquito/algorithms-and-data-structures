/*

                ARRAYS are ORDERED Lists! 
                When to use them:

    - When you need ORDER*;
            *There are more efficient ways to order data, like linked lists;
    - When you need FAST access or insertion and removal

    ==================================================================

                WHEN ARE ARRAYS SLOW?
                Big O of Arrays:
    - Insertion: it depends*;
    - Removal - it depends*;
        * push() and pop() are always faster than shift() and unshift();
    - Searching - O(N);
    - Access - O(1)

    ==================================================================
                BIG O of Array Operations:

    - push - O(1)
    - pop - O(1)
    - shift - O(N)
    - unshift - O(N)
    - concat - O(N)
    - slice - O(N)
    - splice - O(N)
    - sort - O(N*logN)
    - forEach/map/filter/reduce/etc. - O(N)
      
    

*/
